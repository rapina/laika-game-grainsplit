# PRODUCTION LOG / 2026-07-19 / 결 가르기

## 콘셉트 잠금

- 제목: 결 가르기 / Grainsplit
- 질문: 나무가 스스로 튕겨 올라오는 그 순간에 쐐기를 박아 넣어, 갈라짐을 정확히 표시된 자리에서 멈춰 세울 수 있는가.
- 노리는 재미와 감각: 쐐기가 나무의 반동 정점에 정확히 맞아 들어가며 결을 따라 쩍 벌어지는, 손끝에 오는 한 방의 저항감과 멈춰야 할 지점 직전의 조바심.
- 핵심 입력: 떼지 않고 반복하는 탭. 한 통나무당 3~6번. 화면 어디를 눌러도 되고 키보드는 Space. 조준도 드래그도 없다.
- 시스템 반응과 긴장 변화: 통나무가 고유 주기로 요람에서 튕겨 오른다. 반동 정점에서 탭하면 쐐기가 깊이 박히고 균열이 결을 따라 크게 전진하며, 어긋나면 전진량이 줄거나 쐐기가 튕긴다. 균열은 결 노이즈를 따라 휘고 옹이를 만나면 꺾인다. 균열 끝이 초록 띠 안에서 멈추면 성공, 못 미치면 안 갈라진 채 넘어가고, 띠를 넘으면 산산조각이다. 밴드가 올라갈수록 반동 주기가 짧아지고 판정 창과 띠가 좁아지며 결이 더 휘고 옹이가 늘어난다. 규칙 문장은 처음부터 끝까지 같다.
- 재료: 마른 활엽수 통나무. 나이테, 세로 섬유결, 옹이의 뒤틀림, 갈라진 면의 거친 보풀, 튀는 나뭇가루. 균열면에 안쪽 밝은 생재 색이 드러난다.
- 시각 매체: 실시간 파괴 전파. 시드 고정된 결 벡터장 위를 균열 팁이 1/60초 스텝으로 걸어가고, 벌어진 틈은 절차적 목재 섬유 텍스처로 채워진다. 나뭇가루는 판정에 관여하지 않으므로 비결정론이다.
- 대표 색: 세피아 브라운(젖은 목재) + 밝은 생재 크림. 목표 띠와 성공 표시만 연한 수액 그린. 실패 표시는 색이 아니라 형태로도 구분된다.
- 세계: 해 질 녘 장작 마당. 왼쪽에 오늘 쪼갠 것과 망친 것이 함께 쌓이는 장작더미. 지운 것 없이 그대로 남는다.
- 마지막 장면: 마지막 통나무의 균열이 얼어붙은 채 정지하고, 균열 팁과 목표 띠 사이 거리가 화살표와 수치로 찍힌다. 옆의 장작더미가 그 판의 전부를 한 장으로 보여 준다.
- 한 판 길이: 약 90초. 통나무 14개, 5개 난이도 밴드. (숙련 정책 실측 평균 84.7초)
- 제외: 조준, 드래그, 스와이프, 멀티터치, 파워업, 콤보 배수, 상점, 게이지 UI, 소리에만 존재하는 신호, 가이드가 플레이 영역을 덮은 채 시간이 흐르는 것.

깊이는 별도 기능을 붙이지 않고 규칙의 자연 변수만 조여서 만들었다. 반동 주기, 세 겹의 판정 창,
목표 띠 폭, 결 편향, 옹이 수가 밴드마다 함께 좁아지고, 다섯 변수 모두 화면에서 직접 보인다.
같은 탭 하나가 상황에 따라 다른 판단이 되는 지점은 마지막 접근이다. 띠가 가까워지면 오히려
정점을 살짝 비껴 쳐서 전진량을 줄여야 하고, 그마저 안 되면 일부러 빗맞혀 전진을 0으로 만들고
탭 예산을 태우는 선택이 남는다. 전진 배수가 정확도에 연동되므로 "항상 정확하게"가 아니라
"필요한 만큼 정확하게"가 답이 된다.

## 판정과 점수

- 4등급이 한 입력 위에 겹쳐 있다: 빗나감(성공 창 밖, 전진 0, 탭 소모) / 물림(전진 배수 0.6~1.0) / 정타(배수 1.0, 균열이 펴지고 결 편향 절반) / 결(결 창 안이면서 그 탭이 균열을 띠 안에 세운 마지막 탭일 때).
- 정확도는 탭마다 `|오차| / 성공 창`을 0~1로 정규화해 1에서 뺀 값의 평균이다. 빗나감은 0이다.
- 결과 화면은 부호 있는 평균을 진단으로 쓰지 않는다. 대신 흔들림(부호 있는 오차의 표준편차)을 함께 찍어, 양쪽으로 크게 흔들린 판이 "거의 완벽"으로 요약되지 않게 했다. `src/game/grainsplit/rules.test.ts`의 `swing readout`이 이 함정을 그대로 테스트한다.
- 총점 = 쪼갠 통나무 수 x 1,000,000 + round(평균 정확도 x 999) x 1,000 + min(999, 정타+결). 사전식이며 하위 등급 최대 조합(999,999)이 상위 한 단위를 넘지 못한다는 것을 테스트로 고정했다.

### 등급 임계값과 실측 분포

정타/결 창은 GDD의 깊이 표 값을 그대로 유지하고, 실제로 어떤 비율이 나오는지를 관측해 기록했다.
`node scripts/playability-sim.mjs 600` 기준 두 사람 모델을 합친 실측 분포는 다음과 같다.

- 정타 이상(정타+결) 비율: **39.8%** (설계 의도 상위 35%)
- 결 비율: **3.3%** (설계 의도 상위 8%. 결은 창 안에 드는 것 외에 "그 탭이 띠에 세운 마지막 탭"이라는 조건이 하나 더 붙으므로 구조적으로 더 낮다)
- 정책별 정타 이상 비율: 직관 12.3%, 숙련 67.4%

정직하게 적어 두면, 이 분포는 사람이 실제로 친 기록이 아니라 반응 지연 N(200ms, 40ms)에
지각 오차와 운동 오차를 얹은 사람 모델의 관측치다. 이 환경에서 확보할 수 있는 가장 가까운
실기 대체물이며, 사람 플레이 로그로 다시 맞춰야 할 항목으로 남긴다.
최저 등급에 갇히지 않도록, 결이 터지는 조건은 결과 화면에 문장으로 찍힌다
(`결: 결 창 안에서 띠에 세운 탭`). 오차의 방향과 크기는 매 탭 화면에 남는다.

## 채택한 두 가지 플레이 학습

두 항목 모두 **화면 정보만으로** 확인했다. 근거 캡처를 함께 남긴다.

1. **실패 뒤에도 도달한 마지막 게이트와 빗나간 방향이 남아 있어야 한다.** 남지 않으면 서로 다른 시도가 구분되지 않는다.
   - 구현: 실패한 통나무는 균열이 얼어붙은 채 멈추고, 균열 팁과 띠 가장자리 사이에 화살표가 그어지며 `1% 부족 · 밴드 5`처럼 부족/초과와 도달 밴드가 함께 찍힌다. 이 표시는 해소 연출 내내 유지되고 게임 오버 화면까지 그대로 남는다. 망친 조각은 장작더미에서 지워지지 않는다.
   - 확인: `verification/failure-frame.png`와 `verification/game-over.png`. 게임 오버 캡처 한 장만 보고 "마지막 통나무는 밴드 5에서 1% 모자라 멈췄다"를 읽을 수 있다. 코드 경로는 `GrainsplitGame.drawFailureArrow`와 `drawHud`의 `showFailure`이며, `phase === 'over'`에서도 유지되는 것이 요점이다.
2. **방향과 크기를 함께 주는 교정 신호를 조건 없이 항상 보여 준다.** 바로 다음 시도에 적용할 수 있어야 한다.
   - 구현: 마지막 탭의 오차가 링 위 눈금으로 남고 그 옆에 플레이어의 말로 `늦음 +18ms` / `빠름 -82ms`가 찍힌다. 성공했을 때도 실패했을 때도, 등급과 무관하게 항상 나온다. 그 통나무가 끝날 때까지 사라지지 않는다.
   - 확인: `verification/verb-miss.png`(`늦음 +201ms`), `verification/verb-bite.png`(`빠름 -99ms`), `verification/verb-precise.png`(`늦음 +1ms`). 세 캡처 모두 판정 등급이 다른데도 교정 문구가 똑같이 떠 있다. 조건부 표시가 아니라는 증거다.

## 목표 화면 대조

`design/targets/`의 다섯 장을 GDD 직후, 코드 이전에 생성했다(출처와 해시는 ART.md).
검증 캡처와 나란히 놓고 판정한 결과와 의도적으로 다르게 간 부분은 다음과 같다.

- 도달함: 재료 질감(나이테, 섬유결, 옹이, 껍질, 보풀 난 균열면과 드러난 생재), 핵심 동사의 반응(쐐기가 박히고 균열이 결을 따라 전진, 나뭇가루가 튐), 정보 요소(가이드 3줄, 통나무 카운터, 링과 정타 호, 교정 문구, 목표 띠, 쐐기 3개, 장작더미, 거리 수치와 화살표, 다시 (R) 버튼).
- 의도적 차이 1: 목표 화면은 쐐기와 링을 하늘 배경 위에 두고 통나무를 고정해 그렸다. 실제 구현은 **통나무가 요람에서 튕겨 오르고 쐐기는 고정**된다. 콘셉트 잠금의 "나무가 스스로 튕겨 올라오는 그 순간"에 맞추려면 반동의 주체가 나무여야 한다고 판단해 바꿨다. 그 결과 통나무가 반동 정점에서 지평선 위로 조금 올라온다.
- 의도적 차이 2: 목표 화면의 배경은 헛간, 등불, 크레인 같은 소품이 있는 회화적 장면이다. 구현은 절차 그래픽으로 재현 가능한 범위(노을 그라디언트, 장작더미와 침엽수 실루엣, 흙바닥과 부스러기)로 줄였다. 재현 불가능한 회화 연출을 목표로 삼으면 목표가 허구가 되기 때문이다.
- 의도적 차이 3: 목표 화면의 산산조각 장면은 통나무 전체가 여러 조각으로 날아간다. 구현도 쐐기꼴 파편이 회전하며 날아가지만 파편 수는 6~8개로 제한했다. 파편은 판정에 관여하지 않는 연출이라 프레임 안정성을 우선했다.

## 감산

### 1차 (첫 완성본 뒤)

템플릿 셸에서 이 게임의 질문, 조작, 시스템 반응, 재료, 마지막 장면 중 어디에도 기여하지 않는 것을 지웠다.

- 타이틀 메뉴 화면, 랭킹 화면, 로컬 기록(`records.ts`), 모달, MobileFrame
- 광고 일체(`src/ads/`, 배너, 전면 광고 카운터), 인앱 결제(`src/iap/`), 플랫폼 리더보드 어댑터(`src/platform/`)
- BGM 매니저와 오디오 설정, `public/audio/*.mp3` 두 곡. 사운드는 핵심 동사를 설명하는 합성 효과음만 남겼다
- 4개 언어(zh/ja) 번역 테이블과 i18next 셸. 이 게임의 문자열은 ko/en 두 개만 `strings.ts`에 둔다
- 죽은 템플릿 CSS 전부(랭킹, 모달, 타이틀, 버튼 스타일), `NEW_GAME.md`, `SampleGame.ts`, `shareScreenshot`, `useTypewriter`
- 결과: 게임은 마운트 포인트 하나가 됐고, 런타임이 자기 타이틀 카드와 결과 화면과 재시작을 직접 소유한다

### 2차 (남은 요소 점검)

- `logicRng.ts`: 엔진이 자체 시드 RNG를 갖게 되면서 아무도 호출하지 않는 모듈이 됐다. 제거하고, 대신 `?seed=`가 실제로 런을 결정하도록 `__grainsplitSeed`를 런타임에 연결했다. 시드가 있다고 적어 놓고 실제로는 `Math.random()`을 쓰던 상태를 고친 것이다
- `GameCallbacks.onScoreChange`: 아무도 구독하지 않는 콜백
- 템플릿 기본 아이콘 5종: 이 게임의 키 이미지에서 다시 만들었다
- 남긴 것: 화면 우상단의 일시정지/음소거/언어 글리프 3개. 메뉴가 아니라 검증 항목(음소거, 언어 전환, 일시정지)의 터치 접근 경로이고, 키보드 P/M/L과 같은 기능이다

**감산하지 않은 것**: 5개 난이도 밴드, 세 겹 판정 창의 변화, 목표 띠의 이동과 축소, 옹이.
이것들이 숙련 판단을 만드는 구조이므로 감산 대상이 아니다.

### 확보한 시간의 재투자

빈자리를 새 기능으로 채우지 않고, 순서대로 다음에 재투자했다.

1. **핵심 동사의 반응(1순위)**
   - 반동의 주체를 쐐기에서 통나무로 옮겼다. 통나무가 요람에서 실제로 튕겨 오르고, 그 정점에 쐐기를 박는다
   - 쐐기가 실제로 박히는 동작을 넣었다. 등급별로 파고드는 깊이가 다르고(물림 16px, 정타 26px, 결 34px), 빗나감은 쐐기가 기울어 미끄러진다. 빠르게 꽂히고 천천히 빠지는 이징이다
   - 쪼개짐을 두 장의 마스크로 다시 만들었다. 처음에는 마구리면을 통째로 두 번 그려 좌우로 밀어서 유령처럼 겹쳐 보였는데, 균열선을 따라 자른 마스크 두 개로 바꾸니 하나의 통나무가 두 쪽으로 벌어지는 것으로 읽힌다
2. **판정별 시간 연출**: 히트스톱을 등급으로 나눴다(빗나감 24ms / 물림 55ms / 정타 95ms / 결 140ms). 화면 흔들림 폭과 나뭇가루 개수(5/16/34/54)도 함께 간다. 옹이에 걸리면 흔들림이 더해진다
3. **매체 완성도**: 마구리면에 방사형 건조 균열, 변재와 심재 층, 들쭉날쭉한 껍질 테두리를 넣었다. 옹이는 동심 타원과 결 소용돌이로 다시 그려서 과녁처럼 보이던 것을 고쳤다. 통나무는 밴드당 한 번만 그려 캐시하므로 이 디테일이 프레임을 잡아먹지 않는다
4. **사운드 타이밍**: 등급별 음색을 안착의 깊이와 섬유 찢김의 길이로 구분했다(빗나감은 찢김 없이 금속 스침만, 결은 500ms 찢김)
5. **프레임 안정성**: 고정 스텝 누산기, 복귀 프레임 `dt` 100ms 클램프, 정적 마구리면 캐시

## 검증

모든 수치는 저장소에 커밋된 스크립트로 재현할 수 있다.

### 명령과 산출물

| 검증 | 명령 | 산출물 | 결과 |
| --- | --- | --- | --- |
| 결정론 규칙 테스트 | `npm test` | `src/game/grainsplit/*.test.ts` | 3 파일 **55 테스트 통과** |
| 타입 검사 | `npx tsc -b` | 없음 | 통과 |
| 프로덕션 웹 빌드 | `npm run build:web` | `dist/` | 통과, `index-*.js` 471.87 kB / gzip 150.99 kB |
| 자동 플레이 + 재시작 | `npm run smoke` | `smoke-result.json`, `smoke.png` | `mounted`/`finished`/`resultDelivered`/`restartVerified` 모두 true, 콘솔·페이지 오류 0 |
| 뷰포트 + 캔버스 해상도 | `node scripts/viewport-smoke.mjs` | `verification/viewport-result.json`, `verification/viewport-*.png` | 4개 뷰포트 전부 통과 |
| 판정 등급별 캡처 | `node scripts/capture-verbs.mjs` | `verification/capture-result.json`, `verification/*.png` | 4등급 모두 확보, 오류 0 |
| 플레이 가능성 게이트 | `node scripts/playability-sim.mjs 600` | `verification/playability-sim.json` | 7개 게이트 전부 통과 |

### 플레이 가능성 시뮬레이션 (각 600회)

`scripts/playability-sim.mjs`는 엔진 TypeScript를 esbuild로 번들해 **실제 `step`/`tap`을 틱 단위로**
호출한다. 소요 시간은 엔진이 누적한 `ticks x STEP_MS`에서만 읽는다. 판정, 시간, 종료 조건을
닫힌 공식이나 상수로 재유도한 곳은 없다. 정책이 보는 값은 `observe()`가 노출하는 렌더된 것뿐이다
(링의 열림과 닫힘, 통나무 높이, 균열 팁, 칠해진 띠, 교정 문구의 숫자, 남은 탭과 쐐기).
밴드 수치와 기본 전진량은 보이지 않으며, 숙련 정책은 링의 열림 폭과 균열이 움직인 거리를
관측해 스스로 추정한다. 위치 관측에는 지각 오차 N(0, 0.012)를 얹는다.

| 지표 | 직관 정책 | 숙련 정책 |
| --- | --- | --- |
| 완주율 | 0.000 | **0.552** |
| 쐐기 소진 종료 | 1.000 | 0.448 |
| 시간 초과(무정보 종료) | **0.000** | **0.000** |
| 실패 0회로 끝난 판 | 0.000 | 0.028 |
| 쪼갠 통나무 | 3.79 ± 1.19 | 11.50 ± 1.21 |
| 평균 정확도 | 0.309 | 0.708 |
| 전체 탭 중 정타 이상 | 0.123 | 0.674 |
| 결 성공/판 | 0.25 | 3.73 |
| 소요 시간(초) | 36.3 ± 6.4 | **84.7 ± 3.9** |
| 점수 p10 / 중앙 / p90 | 2,351,803 / 4,289,004 / 5,302,106 | 10,705,840 / **12,670,042** / 13,715,045 |

반응 지연 노이즈 모델의 **지정값 대 실현값**:

| 정책 | 지정 | 실현 평균 | 실현 표준편차 | 표본 |
| --- | --- | --- | --- | --- |
| 직관 | N(200, 40) | **200.20** | **40.18** | 19,587 |
| 숙련 | N(200, 40) | **200.01** | **39.94** | 307,918 |

반응 지연용 가우시안과 운동·지각용 가우시안을 별도 생성기로 분리했다. 처음에는 하나로 쓰다가
실현 표준편차가 51.08로 튀는 것을 보고 원인을 찾았다. 지금은 두 정책 모두 지정값과 0.2% 이내로 맞는다.

게이트 판정: 직관 정책은 시간 초과 없이 100% 판정(쐐기 소진)으로 끝나고 한 판에 평균 3.79번
핵심 판정을 겪는다. 실패를 한 번도 쓰지 않고 완주하는 포화는 일어나지 않는다(0.000).
숙련 정책은 완주하되 55.2%에 그치고 정타 비율 67.4%로 포화하지 않는다. 두 정책의 점수 분포는
겹치지 않는다(직관 p90 5.30M < 숙련 p10 10.71M).

직관 정책이 밴드 1~2를 넘기지 못하는 것은 설계대로다. 화면 가이드가 가르치는 "링이 열릴 때"를
그대로 따르면 200ms 반응이 밴드 3부터 성공 창(±92ms) 밖으로 밀려난다. 그때 화면에 뜨는
`늦음 +108ms`가 무엇을 고쳐야 하는지 알려 주고, 반동을 미리 읽는 요령으로 넘어가게 된다.

### 캔버스 해상도 게이트

각 뷰포트에서 `canvas.width / CSS width`와 `canvas.height / CSS height`가 모두
`max(1, devicePixelRatio)` 이상인지 확인한다.

| 뷰포트 | DPR | 버퍼 | CSS | 가로비 | 세로비 | 필요 | 종횡비/설계 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 360x800 | 3 | 1079x2335 | 359.7x778.3 | 3.0001 | 3.000 | 3 | 1.00001 |
| 390x844 | 3 | 1169x2529 | 389.7x843.0 | 3.0001 | 3.000 | 3 | 1.0003 |
| 430x932 | 3 | 1289x2789 | 429.7x929.7 | 3.0001 | 3.000 | 3 | 1.00018 |
| 900x760 (넓은 화면) | 2 | 702x1518 | 351.0x759.0 | 2.000 | 2.000 | 2 | 1.00079 |

렌더러 해상도를 `devicePixelRatio`에만 고정하면 화면이 커져 프레임이 확대될 때 오히려
CSS 픽셀당 디바이스 픽셀이 1 미만으로 떨어진다. 그래서 해상도를 `devicePixelRatio x 표시 배율`로
잡고, CSS 크기를 실제 백버퍼에서 역산해 비율이 정확히 DPR이 되게 했다.
성능 때문에 해상도를 낮춘 곳은 없다. 넓은 화면에서도 uniform scale 레터박스라 원이 원으로 남는다
(종횡비 오차 0.08% 이내).

### 첫 판 가이드 3답 (`verification/first-play.png`)

캡처 한 장만 보고 답할 수 있다.

1. 입력이 무엇을 하는가 → 화면 하단 `탭 = 쐐기 박기`
2. 언제 행동하는가 → `그린 링이 열릴 때`, 그리고 링 안에 `지금`
3. 목표와 종료 조건 → `통나무 14개, 쐐기 3개`, 좌상단 `통나무 1/14`, 요람 옆에 실제로 선 쐐기 3개

가이드는 플레이 영역 바깥 하단 띠에 있고, 첫 탭 전까지 반동이 정지한다
(캡처 시점 상태: `started: false`, `elapsedMs: 0`). 가이드가 화면을 덮은 채 시간이 흐르지 않는다.
노출 규칙은 `engine.test.ts`의 `guide exposure`가 고정한다. 성공 2회까지 유지, 숨긴 뒤 실패 2회
연속이면 재등장.

### 게임 오버 2답 (`verification/game-over.png`)

1. 판이 끝났는가 → 상단 패널에 `끝 · 통나무 10/14`, 평균 정확도와 흔들림, 총점. 진행이 멈추고 마지막 균열이 얼어붙어 있다. 일반 플레이 화면과 혼동할 수 없다
2. 어떻게 다시 시작하는가 → 하단에 큰 버튼 `다시 (R)`

종료 직후 900ms 보호 시간을 둬서 잔여 탭이 결과 화면을 건너뛰지 못한다.
`npm run smoke`가 화면 탭으로 새 판이 실제로 시작되는 것까지 확인한다(`restartVerified: true`).
호스트로 결과가 전달되는 것도 같은 증거에 있다(`resultDelivered: true`,
`verification/capture-result.json`의 `game-over-result`: `{score: 11323008, phase: 5}`).

### 판정 등급별 캡처

네 장이 서로 구분된다. 캡처만 보고 재료가 어떻게 반응했고 어느 등급인지 답할 수 있다.

| 캡처 | 오차 | 등급 | 화면에서 읽히는 것 |
| --- | --- | --- | --- |
| `verb-miss.png` | +201ms | 빗나감 | 링이 닫혀 어둡고, 통나무는 멀쩡하며 균열이 없다(`crackP 0`). `늦음 +201ms` |
| `verb-bite.png` | -99ms | 물림 | 짧고 휜 균열, 좁게 벌어짐, 나뭇가루 적음. `빠름 -99ms` |
| `verb-precise.png` | +1ms | 정타 | 곧고 길게 뻗은 균열, 넓게 벌어진 생재면과 보풀, 나뭇가루가 튐 |
| `verb-grain.png` | +1ms | 결 | 통나무가 두 쪽으로 벌어져 마구리면이 갈라지고, 장작더미에 결 자국이 남는다 |

### 나머지 확인

- 일시정지/복귀: `verification/paused.png` (`paused: true`). 일시정지 중 시뮬레이션이 멈추고, 복귀 프레임의 `dt`를 100ms로 클램프해 시간이 튀지 않는다
- 음소거: `verification/muted.png` (`muted: true`). AudioContext는 첫 입력 뒤에만 생성되므로 그 전에는 완전 무음이다
- 언어 전환: `verification/game-over-en.png`. 플레이 중 전환해도 상태가 보존된다(`strings.test.ts`가 ko/en 키 집합 일치, 빈 값 없음, em/en 대시 없음, 숫자 보존을 고정)
- 재시작: R 키와 화면 탭 양쪽. 초기 상태로 복원된다
- 한 판 완주와 세션 상한: 숙련 정책 평균 84.7초, manifest `maxSeconds` 180 이내. `engine.test.ts`의 `a whole run`이 40초~180초 범위를 고정한다
- 콘솔 오류, 페이지 오류, 실패 요청: 0건 (smoke, viewport, capture 세 스크립트 모두)

## 결과

- 상태: draft
- 게임 잠금: 준비됨. 제작 잠금 차단 사유(임시 자산, 템플릿 문구, 언어 누락, 출처 없는 자산, 결정론 테스트 실패, 화면 넘침, 비율 왜곡, 진행 중 변화 없음, 오버레이 수준의 핵심 동사, 목표 화면 부재, 가이드 3답 미달, 게임 오버 2답 미달, 플레이 가능성 게이트 미달, smoke 증거 부재) 중 남은 것 없음
- 알려진 문제:
  - 정타/결 임계값이 사람 모델 관측치에 맞춰져 있고 실제 사람 플레이 로그로는 아직 재조정하지 않았다. 사람 기록이 생기면 다시 맞춰야 한다
  - 통나무가 반동 정점에 있을 때 지평선 위로 조금 올라온다. 반동의 주체를 나무로 옮기며 생긴 결과이고 콘셉트에는 맞지만, 배경 레이어와의 관계는 다듬을 여지가 있다
  - `scripts/capture-verbs.mjs`는 4등급을 모두 확보할 때까지 반복하므로 실행 시간이 시드에 따라 흔들린다(현재 시드 11에서 2라운드)

## 런치패드 피드백

- 재사용 후보:
  - `scripts/viewport-smoke.mjs`의 캔버스 해상도 게이트. `devicePixelRatio`에만 맞춘 렌더러 해상도가 큰 화면에서 조용히 미달되는 문제는 이 게임만의 것이 아니다. CSS 크기를 백버퍼에서 역산하는 방식과 함께 템플릿으로 올릴 만하다
  - 페이지 안에서 rAF로 도는 캡처 드라이버(`capture-verbs.mjs`의 `__drv`). 밖에서 상태를 폴링하면 왕복 지연 때문에 수 ms짜리 판정 창을 겨냥할 수 없다
  - 시뮬레이션의 노이즈 생성기 분리 원칙. 반응 지연용과 운동·지각용을 한 생성기로 쓰면 실현 분산이 조용히 어긋난다
- 게임에 남길 코드: `src/game/grainsplit/` 전체(밴드 표, 순수 판정, 결정론 엔진)는 이 게임의 것이다. 다만 "순수 규칙 모듈을 먼저 쓰고 렌더러가 그것만 호출한다"는 구조와, 시뮬레이션이 같은 모듈을 esbuild로 번들해 import하는 방식은 그대로 이식할 수 있다
- 다음 게임에서 재검증할 항목:
  - 런타임이 `globalThis.__gameState`를 매 프레임 갱신하는가. 100ms 폴링은 좁은 판정 창을 가진 게임의 검증을 불가능하게 만든다
  - 시드 파라미터가 실제로 런을 결정하는가. 시드를 받는 코드가 있는 것과 그 시드가 게임 난수를 지배하는 것은 다르다
  - 템플릿 셸(광고, IAP, 랭킹, BGM, 4개 언어)을 언제 지우는가. 이번에는 게임이 자기 타이틀과 결과 화면을 소유하기로 정한 시점에 한꺼번에 지웠고, 그 편이 나중에 지우는 것보다 훨씬 쌌다

## 공개 서사 (제작 잠금 뒤 추가)

잠긴 게임을 실제로 빌드해 브라우저에서 한 판 끝까지 플레이한 뒤 작품 노트를 썼다.
`npm run build:web` 결과를 `vite preview`로 띄우고 390x844 dpr2에서 렌더된 캔버스에 포인터
이벤트로 탭해 플레이했다. 결과: 통나무 9/14, 밴드 4, 쐐기 3개 소진, 평균 정확도 0.50,
흔들림 ±107ms, 총점 9,498,019, 51.7초. 마지막 통나무는 띠를 2% 넘겨 산산조각으로 끝났고,
게임 오버 화면에 `2% 초과 · 밴드 4`가 그대로 남았다. 설계 검토가 기록한 산산조각 연출 중
질감 손실도 이 판에서 직접 봤다.

추가한 파일은 `WHY.md`, 라이카 일러스트 원본과 웹 렌디션, 그 프롬프트와 출처 기록이며,
`game.manifest.json`에는 `credits`, `whyCreated`, `media.makerIllustration`만 더했다.
잠긴 제목, 소개, 조작, 세션, 게임 미디어와 게임 소스, 게임 아트, 사운드는 건드리지 않았다.
`design-review.json`은 검토자의 기록이므로 읽기만 했다.

## 잠금 뒤 수정 사이클 (2026-07-19)

아케이드 포털 스모크에서 러너 iframe이 `Current environment does not allow unsafe-eval, please use pixi.js/unsafe-eval module to enable support.`로 부팅에 실패했다. 포털이 적용하는 CSP에 `unsafe-eval`이 없어 PixiJS가 셰이더 프로그램을 만들지 못한다. 로컬 dev와 게임 자체 스모크는 이 CSP 아래서 돌지 않으므로 여기서만 드러났다.

- 수정: `src/arcadeEntry.ts` 최상단에 `import 'pixi.js/unsafe-eval'` 추가. 게임 규칙, 판정, 밸런스, 자산은 바뀌지 않았다.
- 재검증: `npm test` 55개 통과, `npx tsc -b` 통과, `npm run build:web` 통과, `npm run smoke` 통과(`resultDelivered`, `restartVerified` 모두 true, sourceHash 갱신), `node scripts/viewport-smoke.mjs` 통과.
- 재빌드와 `dist-arcade/` 재생성, 아케이드 local-fixture 재동기화, 잠금 갱신(`--relock`)까지 같은 사이클에서 마쳤다.
- 소스가 바뀌었으므로 설계 검토를 새 맥락에서 다시 받았다.

## 잠금 뒤 수정 사이클 2 (2026-07-19)

포털 스모크가 한 판을 완주한 뒤 실패 요청 1건으로 걸렸다. 타이틀 키 이미지를 `document.baseURI` 기준으로 풀고 있었는데, 아케이드 러너는 자기 오리진에서 페이지를 서빙하므로 `/runner/v1/art/title-key.jpg`를 찾아 404와 CORS 오류가 났다. 게임 자체 스모크는 standalone 빌드라 이 경로를 타지 않아 드러나지 않았다.

- 수정: `titleKeyUrl()`을 모듈 자신의 URL(`import.meta.url`) 기준으로 바꾸고 standalone은 절대 경로를 유지했다. 규칙, 판정, 밸런스는 바뀌지 않았다.
- 재검증: `npm test` 55개 통과, `npx tsc -b`, `npm run build:web`, `npm run smoke`(resultDelivered·restartVerified true), `node scripts/viewport-smoke.mjs` 모두 통과.
- 재빌드, `dist-arcade/` 재생성, 아케이드 local-fixture 재동기화, 잠금 갱신, 포털 스모크 재실행까지 같은 사이클에서 마쳤다.

## 잠금 뒤 수정 사이클 3 (2026-07-19) — 핵심 동사 재료 반응

플레이어가 "통나무를 쪼갰는데 실제로 쪼개지지 않는다"고 보고했다. 게시된 `verification/verb-precise.png`가 판정 `늦음 +1ms`인데도 통나무 윗변에 짧은 자국만 남기고 통나무는 통째로 멀쩡해, 캡처만 보고는 재료가 갈라졌다고 말할 수 없었다. `docs/knowledge/CRAFT.md`의 "핵심 동사 직후 재료가 실제로 반응한다" 기준 미달이다.

설계는 그대로다. GDD대로 통나무 하나를 밴드별 계획 탭 3~6회(예산 +2)로 쪼개고, 균열은 탭마다 `ba × 배수 × cos(각도)`만큼 전진하며, 팁이 초록 띠 안에 서는 탭에서 갈라진다. 판정 창, 점수, 실패 예산, 밴드 수치는 한 줄도 건드리지 않았다. 표현만 고쳤다.

- 균열 자체: 폭을 등급별로 키웠다(물림 4, 정타 6.5, 결 9.5). 어두운 틈을 몸통으로 깔고 양쪽 입술에 생재 단면을 좌우 다른 명도로 붙여 깊이를 만들었으며, 팁으로 갈수록 폭이 0으로 좁아지게 해 "지금 내려가는 중"으로 읽히게 했다. 전파 속도도 0.0016 → 0.0034/ms로 올려 타격과 균열이 한 사건으로 보이게 했다.
- 마지막 탭: 균열이 띠에서 멈추지 않고 통나무 아래로 **관통해 빠져나간다**(`pendingP = 1`, 표현 전용. 엔진의 `crackP`와 판정은 그대로다). 관통이 끝난 뒤에야 반쪽이 움직인다.
- 갈라진 뒤: 26px 옆으로 미끄러지던 것을 요람 접촉점을 축으로 **벌어지고, 바깥으로 기울고, 떨어지는** 운동으로 바꿨다(슬라이드 24px, 기울기 0.14rad, 낙하 30px). 마스크를 반쪽의 자식으로 옮겨 이음선이 조각을 따라 움직이게 했고, 벌어진 면에는 나뭇결 단면과 찢긴 보풀을 반쪽 안에 그려 넣어 조각과 함께 기울고 떨어진다.
- 등급 구분: 히트스톱 24/55/95/140ms, 흔들림 3/6/9/13, 나뭇가루 8/30/62/96개에 더해 균열 폭과 전진량이 눈으로 갈린다. 나뭇가루는 쐐기가 무는 윗면과 균열 팁 두 곳에서 따로 튄다.
- 산산조각(검토 기록 p20): 파편이 단색 다각형이던 것을 고쳐 원래 원반의 나이테와 껍질을 그대로 지니게 했고, 화면 밖으로 날아가 버리던 것을 요람 위로 떨어져 쌓이게 했다. 판이 끝난 마지막 화면이 빈 요람이 아니라 부서진 나무로 남는다.

의도적 차이 4: 목표 화면 `design/targets/verb-precise.png`의 균열은 밝은 호박색으로 빛나고 통나무 한참 아래까지 내려가 있다. 구현의 균열은 어두운 틈에 생재 입술을 붙인 형태다. 빛나는 균열은 이 게임의 재료(마른 활엽수)가 아니라 달군 쇠에 가깝고, GDD가 정한 "벌어진 틈은 절차적 목재 섬유 텍스처로 채워진다"와도 어긋나 채택하지 않았다. 길이 차이는 연출이 아니라 규칙이다. 목표 화면은 통나무 6/14의 여러 번째 탭이고 게시 캡처는 통나무 1/14의 첫 탭이라, 계획 탭 3회 중 1회분만 전진해 있는 것이 정상이다.

의도적 차이 5: 목표 화면은 결 등급에서 불티가 튄다. 구현은 나뭇가루만 튄다. 쐐기와 나무 사이에서는 불티가 나지 않는다.

- 캡처 타이밍: `scripts/capture-verbs.mjs`가 고정 지연으로 찍던 것을 화면 상태를 기다려 찍도록 바꿨다. Playwright 왕복이 수백 ms라 고정 지연으로는 1100ms짜리 쪼개짐 연출을 놓치고 다음 통나무를 찍고 있었다. 이를 위해 디버그 상태에 `outcome`, `resolveMs`, `splitOpen`, `crackDrawnP`를 노출했다. 결 등급은 반쪽이 요람에서 벌어진 시점(`splitOpen >= 0.8`)에, 나머지 등급은 그 한 방의 균열이 다 그려진 시점에 찍는다. 아래 등급 캡처는 통나무를 끝낸 탭을 쓰지 않아 등급별 반응 크기가 서로 비교된다.
- 재검증: `npm test` 55개 통과, `npx tsc -b` 통과, `npm run build:web` 통과, `npm run smoke` 통과(`resultDelivered`·`restartVerified` true, sourceHash `ac41774e95ef7bdafc9112991986b3d1c5e4136faf37fe88b298321347324313`), `npm run build:arcade` 통과(18개 파일 검증).
- `game.manifest.json`의 `media.screenshots`가 가리키는 4장(`first-play`, `verb-precise`, `verb-grain`, `game-over`)을 현재 빌드로 다시 찍었다.

목표 화면 대조 결과: `design/targets/verb-precise.png`와 `verb-success.png`는 둘 다 통나무가 아직 통째로 있고 균열만 내려가는 프레임이다. 즉 목표 화면 자체에는 "쪼개진 통나무"가 없다. 균열 묘사(어두운 틈 + 양쪽에 삐져나온 생재 보풀)는 구현이 목표에 닿았고, 갈라진 뒤의 반쪽 운동은 목표 화면에 그려진 적이 없어 구현이 목표를 넘어선다. 플레이어 보고의 원인이 여기에 있었다. 목표 화면이 핵심 동사의 완료 상태를 한 번도 그리지 않았으므로, 그 목표를 맞춘 구현도 쪼개지지 않는 것처럼 보였다.

## 잠금 뒤 수정 사이클 4 (2026-07-19) — 등급을 이름으로 부르기

설계 검토가 `blocked`를 냈다. FATAL은 p10 하나다. `strings.ts`에 빗나감·물림·정타·결 / MISS·BITE·TRUE·GRAIN이 두 언어 모두 정의되어 있는데 렌더 코드가 한 번도 쓰지 않아 죽은 문자열이었다. 탭 직후 화면에 뜨는 글자는 `빠름 -116ms` 같은 오차 수치뿐인데, 정작 결과 화면은 `정타·결 1회`라고 세고 결 규칙 문장까지 적는다. 플레이 중에 이름조차 불리지 않는 등급을 결과 화면에서만 세는 셈이라, 무엇을 잘해야 등급이 오르는지 화면에서 배울 수 없었다. sequence 11·13에서 같은 결함이 이미 두 번 나왔다.

판정은 한 줄도 건드리지 않았다. `bands.ts`, `engine.ts`, `rules.ts`는 이 사이클에서 바이트 단위로 그대로다(`git diff --exit-code`로 확인). 창 폭, 점수, 실패 예산, 밴드 수치 모두 그대로고 표현만 고쳤다.

- 등급 호명: 쐐기가 박히는 순간 통나무 위 하늘에 등급 이름이 자기 색으로 튀어나오고, 그 통나무가 해결될 때까지 남는다. 결과 화면이 아니라 플레이 중이며, 두 언어 모두 같은 자리에서 같은 정보를 준다. 반응이 화면에 있는 동안 그 이름도 함께 있어야 하므로 쪼개짐·실패 연출 내내 유지된다.
- 사다리: 이름 아래에 `빗나감 물림 정타 결` 네 칸을 깔고 방금 받은 칸을 자기 색으로 밝힌다. 한 칸 위에 무엇이 있는지가 같은 프레임에 있어야 "무엇을 잘해야 오르는지"가 읽힌다. 언어마다 글자 폭이 달라 위치는 측정해서 가운데로 맞춘다.
- 교정 문구도 해결 연출 동안 유지하도록 바꿨다. 등급 이름은 남는데 오차 수치만 사라지면 둘이 짝이라는 것이 끊긴다.
- 인접 등급 분리(검토 기록 p9): 물림과 정타를 폭 4 대 6.5로만 갈라 놓아, 참조 없이 한 프레임만 보면 판별할 근거가 없다는 지적이었다. 양이 아니라 **종류**를 갈랐다. 정타 미만은 어두운 실금이라 생재가 드러나지 않고(입술 색이 나무 그늘색, 보풀 없음), 정타부터는 틈이 벌어져 창백한 단면이 그대로 보인다. 폭도 2.4 / 8.5 / 12로 벌렸다. 균열 점마다 그때의 등급을 기억하므로, 넓고 창백한 구간이 앞선 좁고 어두운 구간 바로 위에 같은 프레임 안에 놓인다. 비교 대상이 화면 안에 생긴다.
- 같은 사다리를 타격감에도 적용했다: 히트스톱 22/48/105/152ms, 흔들림 3/5/10/14, 나뭇가루 6/24/80/124개, 쐐기가 박히는 깊이 2/11/29/40px.
- 등급 표현 표(`TIER_COLOR`, `TIER_LADDER`, `CRACK_FACE`, `STRIKE_FEEL`, `gradeLabel`)를 `src/game/grainsplit/grades.ts`로 빼 순수 모듈로 만들었다. 죽은 문자열이 다시 생기지 않도록 `grades.test.ts`가 두 언어의 이름 존재, 인접 등급의 종류·폭·깊이 분리, 그리고 런타임이 실제로 라벨을 소비하며 그 호출이 결과 화면이 아니라 플레이 단계에 걸려 있는지를 검사한다.
- 캡처 자체 검증: `scripts/capture-verbs.mjs`가 디버그 상태의 `gradeLabel`·`gradeShown`·`gradeLadder`(그 프레임에 실제로 칠해진 글자)를 읽어, 네 등급 캡처와 두 언어 캡처에 등급 이름이 없으면 실패로 종료한다. 타이밍은 그대로 `waitFor` 기반이며 고정 지연은 쓰지 않았다. 새 캡처 `grade-name-ko.png`(정타), `grade-name-en.png`(TRUE)를 추가했다.
- 재검증: `npm test` 61개 통과, `npx tsc -b` 통과, `npm run smoke` 통과(`resultDelivered`·`restartVerified` true, sourceHash `fc59b7a74f33a36ade4a0a145450d2a6a19c0ffe6540176711a5bc850e8ab527`), `node scripts/capture-verbs.mjs` 통과(네 등급 모두 확보, 콘솔 오류 없음, 등급 이름 ko `정타` / en `TRUE` 확인), `npm run build:arcade` 통과(18개 파일 검증).
- `design-review.json`은 검토자의 기록이므로 읽기만 했다.
